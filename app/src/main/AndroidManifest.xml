<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobility">

    <uses-permission android:name="android.permission.INTERNET" />
    <!--    <uses-permission android:name="android.permission.CAMERA" />-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mobility"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".VerificareRaft"
            android:exported="true"
            android:label="@string/title_activity_verificare_raft"
            android:theme="@style/Theme.Mobility.NoActionBar" />
        <activity
            android:name=".Settings"
            android:exported="true" />
        <activity
            android:name=".ReceptieFurnizorMain"
            android:exported="true" />
        <activity
            android:name=".ReceptieFurnizor"
            android:exported="true" />
        <activity
            android:name=".ReceptieLista"
            android:exported="true" />
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
